{
  "package_id": "{{ package_id }}",
  "package_type": "standard",
  "version": "0.1.0",
  "schema_version": "1.2",
  "spec_id": "{{ spec_id | default('') }}",
  "plane_id": "{{ plane_id | default('ho3') }}",
  "install_targets": [
    {
      "namespace": "{{ namespace | default('lib') }}",
      "target_id": "{{ target_id }}",
      "files": []
    }
  ],
  "assets": [],
  "dependencies": {{ dependencies | default([]) | tojson }},
  "metadata": {
    "created_at": "{{ timestamp }}",
    "author": "{{ author | default('') }}",
    "description": "{{ description | default('') }}"
  }
}
