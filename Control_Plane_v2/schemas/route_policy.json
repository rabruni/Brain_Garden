{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "route_policy.json",
  "title": "Route Policy",
  "description": "Schema for router_policy.json configuration",
  "type": "object",
  "properties": {
    "max_llm_calls_per_session": {
      "type": "integer",
      "minimum": 0,
      "default": 10,
      "description": "Maximum LLM calls allowed per session"
    },
    "llm_deny_list": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Query types that are never allowed LLM"
    },
    "llm_allow_list": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Query types that are always allowed LLM (if capability exists)"
    },
    "required_capabilities": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      },
      "description": "Required capabilities for each query type"
    },
    "custom_handlers": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "Custom handler overrides for query types"
    }
  }
}
