spec_id: SPEC-ROUTER-001
title: "Router Extensions"
framework_id: FMWK-100
status: active
version: 1.0.0
plane_id: ho3

assets:
  - modules/router/prompt_router.py

invariants:
  - "Router decisions MUST be deterministic"

acceptance:
  tests:
    - "pytest tests/test_router*.py -v"
