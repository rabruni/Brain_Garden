spec_id: SPEC-CORE-001
title: "Core Infrastructure"
framework_id: FMWK-000
status: active
version: 1.0.0
plane_id: ho3

assets:
  - lib/paths.py
  - lib/output.py
  - lib/cursor.py

interfaces:
  - name: get_control_plane_root
    description: Return the control plane root directory
  - name: get_repo_root
    description: Return the repository root directory

invariants:
  - "Path resolution MUST be deterministic"
  - "All paths MUST be absolute"

acceptance:
  tests:
    - "pytest tests/test_paths.py"
  checks:
    - "python3 -c 'from lib.paths import CONTROL_PLANE; print(CONTROL_PLANE)'"

non_goals:
  - "This spec does NOT handle file I/O operations"
  - "This spec does NOT handle path validation"
