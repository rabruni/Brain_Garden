{
  "schema_version": "1.0",
  "description": "Ed25519 keys for changeset signing (separate from WO approval keys)",
  "keys": [
    {
      "key_id": "build-001",
      "algorithm": "Ed25519",
      "public_key_b64": "PLACEHOLDER_GENERATE_WITH_wo_keygen.py",
      "created_at": "2026-02-03T00:00:00Z",
      "expires_at": null,
      "roles": ["signer"],
      "description": "Build signing key for G5 changeset attestations"
    }
  ],
  "notes": {
    "key_generation": "Use scripts/wo_keygen.py to generate Ed25519 keypair",
    "private_key_storage": "Store private key securely in ~/.control_plane_v2/build_key.pem",
    "separation": "Signing keys are separate from WO approval keys for role separation",
    "roles": {
      "signer": "Can sign changesets via G5 gate attestations"
    }
  }
}
