spec_id: SPEC-GENESIS-001
title: Bootstrap
framework_id: FMWK-000
status: active
version: 1.0.0
plane_id: hot
assets:
- HOT/scripts/genesis_bootstrap.py
- HOT/config/bootstrap_sequence.json
- HOT/config/seed_registry.json
- HOT/schemas/package_manifest_l0.json
- HOT/tests/test_genesis_bootstrap.py
interfaces:
- name: bootstrap_control_plane
  description: Bootstrap control plane from scratch
invariants:
- Genesis bootstrap MUST be idempotent
- Genesis bootstrap MUST create minimal viable state
- Genesis bootstrap is ONLY exemption from package-manager requirement
acceptance:
  checks:
  - python3 scripts/genesis_bootstrap.py --help
non_goals:
- This spec does NOT handle incremental bootstrap
- This spec does NOT handle bootstrap validation
notes:
  genesis_exception: 'genesis_bootstrap.py is the ONLY script exempt from package-manager
    requirement. It MAY exist without a governing Work Order. It MUST
    only be invoked during initial setup.
    '
