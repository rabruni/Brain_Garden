{
  "package_id": "{{ package_id }}",
  "package_type": "agent",
  "version": "0.1.0",
  "schema_version": "1.2",
  "framework_id": "{{ framework_id }}",
  "spec_id": "{{ spec_id | default('') }}",
  "plane_id": "{{ plane_id | default('ho1') }}",
  "install_targets": [
    {
      "namespace": "agents",
      "target_id": "{{ target_id }}",
      "files": []
    }
  ],
  "assets": [],
  "capabilities": {{ capabilities | default([]) | tojson }},
  "dependencies": {{ dependencies | default([]) | tojson }},
  "metadata": {
    "created_at": "{{ timestamp }}",
    "author": "{{ author | default('') }}",
    "description": "{{ description | default('') }}"
  }
}
